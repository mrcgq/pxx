# .golangci.yml
run:
  timeout: 5m
  modules-download-mode: readonly

linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - staticcheck
    - unused
  disable:
    - ineffassign  # 临时禁用，待代码修复后可重新启用

issues:
  exclude-use-default: false
  max-issues-per-linter: 0
  max-same-issues: 0
